import bataille from "./bataille"
import solitaire from "./solitaire"

export default { solitaire }

/**
 * @typedef {Object} Card
 * @property {"hearts"|"spades"|"diamonds"|"clubs"} suit
 * @property {number} rank
 * @property {boolean} hidden
 */
